/**
 * Production environment.
 * Replaced at build time by the Angular CLI file replacement pipeline when needed.
 */
export const environment = {
  production: true,
  appName: 'Docker Project Manager',
  version: '0.1.0',
} as const;
